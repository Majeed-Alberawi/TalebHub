import { STUDENT_CONFIG } from "../../constants/studentTabs";
import UniversalDashboard from "../../UniversalDashboard";

export default function StudentDashboard() {
  return <UniversalDashboard config={STUDENT_CONFIG} />;
}
