import { TEACHER_CONFIG } from "../../constants/teacherTabs";
import UniversalDashboard from "../../UniversalDashboard";

export function TeacherDashboard() {
  return <UniversalDashboard config={TEACHER_CONFIG} />;
}
